import { GlobalConfig } from "payload/types";

const CompanyInfo: GlobalConfig = {
  slug: "company-info",
  fields: [
    {
      name: "policy",
      type: "richText",
    },
  ],
};

export default CompanyInfo;
